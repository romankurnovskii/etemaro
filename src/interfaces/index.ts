/**
 * Interfaces layer — entry points for the Meridian application.
 */

export { Daemon, getDeterministicCloseRule, type DaemonAdapters } from "./Daemon.js";
export { Cli, type CliAdapters } from "./Cli.js";
